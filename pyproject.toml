[project]
name = "dara"
# Bookkeeping only: this is a virtual workspace root that is never built or published.
version = "1.29.10"
requires-python = ">=3.10.0,<3.13.0"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "packages/dara-core",
    "packages/dara-components",
    "packages/create-dara-app",
    "packages/demo-app",
]